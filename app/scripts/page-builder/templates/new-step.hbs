<h1>New Lesson Builder</h1>

<div class="row">
    <div class="col-md-5">
        <form class="form-horizontal">
            <div class="form-group">
                <label for="stepName" class="col-sm-3 control-label">Step Name</label>
                <div class="col-sm-8">
                  <input type="text" class="form-control" id="stepName" placeholder="The new step name">
                </div>
            </div>
            <div class="form-group">
                <label for="pageType" class="col-sm-3 control-label">Page Type</label>
                <div class="col-sm-6">
                    <select id="pageType" class="form-control">
                        <option>Please select a page type</option>
                        <option value="MultipleChoice">Multiple Choice</option>
                    </select>
                </div>
            </div>
            <div id="stepContent"></div>
            <button type="submit" id="create-lesson" class="btn btn-primary">Generate</button>
        </form>
    </div>
    <div class="col-md-7">
        {{#if output}}
        <p>
            Add the following code below the last lesson item in the <span class="label label-default">imsmanifest.xml</span> file.
        </p>
        <pre><code class="xml">{{ output }}</code></pre>
        {{else}}
        <div class="alert alert-info" role="alert">Fill out the fields on the left to view the generated code XML.</div>
        {{/if}}
    </div>
</div>
